extends layout

block content
    #wrapper 
      #header
        h1 Chat
        #logout-wrapper
          a(href='#')
            img#logoutIcon(src='/images/icons/logout.png', alt='logoutIcon')
          #settingsMenu
            div
              img(src="/images/userimages/"+user._id+".jpg", alt="userimages"+user._id)
              | This is text
      #mainframe
        // Contactlist 
        #left-panel
          #searchuser-wrapper
            #searchIcon-wrapper
              img(src='/images/icons/search.png', alt='searchIcon')
            #searchInput-wrapper
              input#searchInput-js
            #addUser-wrapper
              a#addUser-js.tooltip(href='javascript:;', data-tip='Add User')
          #contactlist-wrapper
            ul#contactlist
              - each contact in contacts
                li(id="uid"+contact._id).offline
                  img(src="/images/userimages/"+contact._id+".jpg", alt="userimages"+contact.name)
                  .userinfo
                    strong.username= contact.username
                    p.availability • Offline            

        // Chatwindow 
        #right-panel
          #conversation-wrapper
            ul#conversation
                        
          // Chatinput 
          #chatinput-wrapper
            input#chatinput-js
      
      input(type='hidden', id='userId', value='#{user._id}')
      input(type='hidden', id='userName', value='#{user.username}')
      
      // Scripts
      script(src='/javascripts/lib/jquery-1.7.2.min.js', type='text/javascript')
      script(src='/javascripts/client.js', type='text/javascript')
      script(src='/javascripts/script.js', type='text/javascript')
